<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Dashboard Usuario</title>
</head>
<body>
    <header>
        {{!-- Navbar --}}
        {{> navbarDashboard }}
    </header>

    <section class="container mt-5">
        {{#if message}}
        <div class="alert alert-success">{{ message }}</div>
        {{/if}}
        <article class="row">
            <div class="col-md-6">
                <ul class="list-group">
                    {{#each formattedResponse }}
                    <li class="list-group-item">
                        {{this.task_name}} - {{this.formattedDate}}
                        <button class="btn btn-danger btn-sm float-end"><a href="/dashboard/{{this.id_task}}" class="list-group-item list-group-item-action bg-danger text-white">Eliminar</a></button>
                        <button class="btn btn-warning btn-sm float-end me-3"><a href="/dashboard?id={{this.id_task}}&task={{this.task_name}}" class="list-group-item list-group-item-action bg-warning">Editar</a></button>
                    </li>
                    {{/each}}
                </ul>
            </div>

            <div class="col-md-6">
                <h3>Agregar Tarea</h3>
                <form method="post" action="/dashboard">
                    <input type="text" name="todo" placeholder="Ingrese Tarea" class="form-control mb-2">
                    <button class="btn-dark w-100 p-2">Agregar</button>
                </form>
            </div>
        </article>
    </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</body>
</html>